import ComingSoon from "../../components/ComingSoon";

export default function BracketTab() {
  return (
    <ComingSoon
      icon="git-network-outline"
      title="Bracket & standings"
      detail="The live bracket for tonight's tournament — group tables first if the format has a group stage, then the elimination tree. Read-only for players; staff get seeding and override controls in the same view."
      milestone="Milestone 8"
    />
  );
}
