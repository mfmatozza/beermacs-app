{
  "name": "@beermacs/db",
  "version": "0.0.0",
  "private": true,
  "description": "Prisma schema, migrations, seed and the Neon client singleton for Beermacs",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "postinstall": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "seed": "tsx prisma/seed.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@beermacs/config": "*",
    "@types/pg": "^8.20.0",
    "eslint": "^10.7.0",
    "prisma": "^7.8.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3"
  }
}
